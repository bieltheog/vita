import{getInstallments}from'@/lib/data';import{effectiveInstallmentStatus,money,daysOverdue}from'@/lib/finance';export default async function P(){const i=await getInstallments();const a=i.filter(x=>effectiveInstallmentStatus(x)==='ATRASADO');return <><div className="page-head"><div><div className="eyebrow">Central</div><h1>Notificações</h1></div></div><div className="card"><div className="list">{a.map(x=><div className="list-row" key={x.id}><div><strong>⚠️ {x.client?.name} está em atraso</strong><div className="person-meta">{daysOverdue(x.due_date,x.remaining_amount)} dias · {x.loan?.loan_code}</div></div><strong>{money(x.remaining_amount)}</strong></div>)}</div></div></>}
