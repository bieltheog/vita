import{getInstallments,getCurrentProfile}from'@/lib/data';import{CalendarBoard}from'@/components/calendar/calendar-board';import{PaymentForm}from'@/components/forms/payment-form';
export default async function P(){const[i,p]=await Promise.all([getInstallments(),getCurrentProfile()]);return <><div className="page-head"><div><div className="eyebrow">Agenda financeira</div><h1>Calendário de recebimentos</h1><div className="muted">Veja quem paga, quanto entra e o que está atrasado.</div></div><PaymentForm installments={i} demo={p?.demo} compact/></div><CalendarBoard installments={i}/></>}
